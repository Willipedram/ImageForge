<div dir="rtl">

# راهنمای ساده اجرای ImageForge در ویندوز

این راهنما برای وقتیه که فقط می‌خوای برنامه رو دانلود کنی و با دابل‌کلیک اجراش کنی. برای نسخه آماده لازم نیست Python یا ابزار برنامه‌نویسی نصب کنی.

## چیزهایی که لازم داری

- ویندوز ۱۰ یا ۱۱ نسخه ۶۴ بیتی
- فایل دانلودشده `ImageOptimizer-Windows-x64.zip`
- یک پوشه که اجازه نوشتن داخلش داشته باشی؛ مثلاً پوشه Documents یا Desktop

## ۱. نسخه آماده را دانلود کن

1. وارد صفحه پروژه در GitHub شو.
2. بالای صفحه روی **Actions** کلیک کن.
3. از سمت چپ، **Windows executable** را انتخاب کن.
4. جدیدترین اجرای موفق را باز کن؛ اجرای موفق کنار اسمش تیک سبز دارد.
5. پایین صفحه، در قسمت **Artifacts**، روی `ImageOptimizer-Windows-x64` کلیک کن.
6. فایل ZIP دانلود می‌شود.

> اگر بخش Artifacts را نمی‌بینی، احتمالاً هنوز Build موفقی انجام نشده یا برای دیدنش باید وارد حساب GitHub شوی.

## ۲. فایل را از حالت فشرده خارج کن

1. روی فایل `ImageOptimizer-Windows-x64.zip` راست‌کلیک کن.
2. گزینه **Extract All...** را بزن.
3. یک مسیر ساده مثل `Documents\ImageOptimizer` انتخاب کن.
4. روی **Extract** کلیک کن.

برنامه را مستقیم از داخل فایل ZIP اجرا نکن. اول حتماً آن را Extract کن.

## ۳. برنامه را اجرا کن

1. پوشه‌ای را که Extract کردی باز کن.
2. روی `ImageOptimizer.exe` دابل‌کلیک کن.
3. چند لحظه صبر کن تا پنجره برنامه باز شود. اجرای اول ممکن است کمی بیشتر طول بکشد.

کنار فایل برنامه، پوشه‌ای به اسم `ProjectData` ساخته می‌شود. اطلاعات کارها، تنظیمات، گزارش‌ها و Backupها داخل همین پوشه نگهداری می‌شوند. این پوشه را پاک نکن و بهتر است برنامه را داخل یک پوشه قابل‌نوشتن اجرا کنی.

## اگر پیام Windows protected your PC دیدی

نسخه فعلی ممکن است امضای دیجیتال ویندوز نداشته باشد و SmartScreen هشدار بدهد. فقط وقتی ادامه بده که فایل را از صفحه رسمی همین پروژه گرفته‌ای و checksum آن درست است:

1. روی **More info** کلیک کن.
2. اسم برنامه را بررسی کن؛ باید `ImageOptimizer.exe` باشد.
3. اگر از منبع فایل مطمئنی، روی **Run anyway** کلیک کن.

اگر فایل را از جای ناشناس گرفته‌ای، اجراش نکن.

## بررسی سالم بودن فایل (اختیاری ولی پیشنهادشده)

فایل دانلودی یک فایل به اسم `ImageOptimizer.exe.sha256` هم دارد. برای بررسی:

1. داخل پوشه برنامه، روی فضای خالی **Shift + راست‌کلیک** کن و **Open in Terminal** را بزن.
2. این دستور را اجرا کن:

```powershell
(Get-FileHash .\ImageOptimizer.exe -Algorithm SHA256).Hash.ToLower()
```

3. عددی که نمایش داده می‌شود را با عدد داخل `ImageOptimizer.exe.sha256` مقایسه کن.
4. اگر دقیقاً یکی نبودند، فایل EXE را اجرا نکن و دوباره از GitHub دانلودش کن.

## اجرای دفعات بعد

برای اجرای دوباره فقط روی `ImageOptimizer.exe` دابل‌کلیک کن. می‌توانی روی فایل راست‌کلیک کنی و با **Send to > Desktop (create shortcut)** یک میانبر روی دسکتاپ بسازی.

برای آپدیت برنامه، EXE جدید را دانلود و جایگزین فایل قبلی کن، ولی پوشه `ProjectData` را نگه دار تا اطلاعات قبلی از بین نرود. بهتر است قبل از جایگزینی، یک کپی از `ProjectData` داشته باشی.

## اگر برنامه باز نشد

- مطمئن شو فایل را از ZIP خارج کرده‌ای.
- برنامه را به یک مسیر ساده و قابل‌نوشتن مثل `Documents\ImageOptimizer` منتقل کن.
- اگر آنتی‌ویروس فایل را مسدود کرده، اول checksum و منبع دانلود را بررسی کن؛ آنتی‌ویروس را بی‌دلیل خاموش نکن.
- یک بار ویندوز را Restart کن و دوباره امتحان کن.
- اگر هنوز باز نشد، همراه با نسخه ویندوز و عکس خطا در صفحه **Issues** پروژه گزارش بده.

## اگر فقط دسترسی DirectAdmin داری

نام کاربری و پسورد صفحه DirectAdmin به‌تنهایی یک اتصال فایل نیست. پورت `2222` یا `2223` هم مخصوص پنل تحت وب DirectAdmin است و نباید آن را به‌عنوان پورت FTP یا SFTP وارد کنی.

1. وارد DirectAdmin شو و بخش **FTP Management** یا **FTP Accounts** را باز کن.
2. یک حساب FTP بساز یا اطلاعات حساب FTP موجود را بردار.
3. در ImageForge، گزینه **FTP** یا ترجیحاً **FTPS** را انتخاب کن و پورت را روی `21` بگذار.
4. Host، نام کاربری و پسورد FTP را وارد کن؛ این اطلاعات ممکن است با ورود پنل DirectAdmin فرق داشته باشند.
5. برای **Remote root** اول `/` را امتحان کن. اگر سایت پیدا نشد، `/public_html` یا `/domains/نام‌دامنه/public_html` را وارد کن.

بعضی حساب‌های FTP به‌صورت **chroot** ساخته می‌شوند؛ یعنی پوشه اصلی سایت برای آن حساب همان `/` است و اجازه دیدن مسیر کامل `/domains/...` را ندارد. اگر پیام `Stopped at directory_listing` یا `not permitted to list this remote directory` دیدی، مقدار **Remote root** را روی `/` بگذار. برنامه هم حالا در صورت بسته‌بودن مسیر واردشده، `/` و مسیرهای متداول دیگر را خودکار امتحان می‌کند و مسیر قابل‌دسترسی را داخل فیلد Remote root می‌گذارد.

پورت پنل ممکن است `2222` یا `2223` باشد؛ هیچ‌کدام پورت FTP نیستند. برای اطلاعاتی شبیه اکانت `pedram@safirezaman.com` باید همان نام کاربری کامل دارای `@` را در برنامه وارد کنی، Host را از شرکت هاست یا صفحه FTP بگیری و معمولاً پورت `21` را انتخاب کنی.

به **مسیر دسترسی** حساب FTP هم دقت کن. اگر حساب روی گزینه «public_html زیر شاخه» مثل `public_html/pedram` تنظیم شده باشد، فقط همان زیرپوشه را می‌بیند و ImageForge نمی‌تواند وردپرس کل دامنه را پیدا کند. برای دسترسی به کل سایت، در DirectAdmin یکی از این تنظیم‌ها را انتخاب کن:

- **شاخه دامین** و سپس در ImageForge مقدار Remote root را `/public_html` بگذار؛ یا
- **مسیر دلخواه** را روی `/home/USERNAME/domains/DOMAIN/public_html` بگذار و در ImageForge ابتدا `/` را امتحان کن.

`USERNAME` و `DOMAIN` را با مقادیر واقعی اکانت خودت جایگزین کن. دسترسی حساب FTP را فقط به اندازه لازم باز کن و آن را روی `/home` یا ریشه کل سرور قرار نده.

گزینه **SFTP** فقط وقتی کار می‌کند که شرکت هاست دسترسی SSH را برای حسابت فعال کرده باشد. اگر فقط پنل DirectAdmin داری و SSH فعال نیست، SFTP وصل نمی‌شود. در این حالت از داخل DirectAdmin حساب FTP/FTPS بساز یا از پشتیبانی هاست بخواه دسترسی لازم را فعال کند.

## پیدا کردن مرحله‌ای که خطا داده

اگر اتصال یا یکی از فرایندها متوقف شود، برنامه آخرین مرحله را با عبارت `[KEYPOINT]` داخل فایل زیر ثبت می‌کند:

```text
ProjectData\logs\imageforge.log
```

در صفحه **Logs** هم می‌توانی رویدادهای Job را ببینی. داخل فایل لاگ دنبال `status=STOPPED` بگرد؛ مقدار جلوی `checkpoint=` دقیقاً نشان می‌دهد خطا در کدام مرحله بوده است. مثلاً این خط یعنی اتصال اولیه برقرار نشده:

```text
[KEYPOINT] status=STOPPED checkpoint=connection error_type=ConnectionFailed
```

مرحله‌های موفق هم با `status=PASSED` ثبت می‌شوند، پس می‌توانی آخرین مرحله موفق قبل از خطا را پیدا کنی. پسورد و اطلاعات محرمانه داخل این پیام‌های Keypoint نوشته نمی‌شوند.

هر بار که دکمه اتصال را می‌زنی یک شناسه کوتاه در `context` با نام `run` ثبت می‌شود؛ با جست‌وجوی همان شناسه فقط خط‌های مربوط به همان تلاش را می‌بینی. بخش `context` مسیر بررسی‌شده، تعداد ورودی‌ها، نزدیک‌ترین پوشه به وردپرس، Markerهای گم‌شده و پیشنهاد مرحله بعد را نشان می‌دهد. نمونه:

```text
[KEYPOINT] status=STOPPED checkpoint=website_discovery context={"closest_path":"/public_html","directories_checked":"8","missing_markers":"wp-admin, wp-includes","next":"check DirectAdmin FTP account root","run":"a1b2c3d4e5f6"}
```

برای دیدن مسیر جست‌وجو، در لاگ عبارت `checkpoint=website_discovery_directory` را پیدا کن. برای هر پوشه، آدرس و عمق پوشه، تعداد ورودی‌ها، زیرپوشه‌هایی که وارد صف جست‌وجو شده‌اند، Markerهای پیدا‌شده و Markerهای گم‌شده ثبت می‌شود. نام فایل‌های عادی ثبت نمی‌شوند و حداکثر ۲۵۰ پوشه Trace می‌شود تا لاگ بی‌نهایت بزرگ نشود.

اگر نتیجه `empty_directories` شامل `/public_html` بود، یعنی برنامه این پوشه را پیدا کرده ولی حساب FTP آن را کاملاً خالی می‌بیند. در این حالت مشکل از الگوریتم جست‌وجو نیست؛ در File Manager دایرکت‌ادمین فایل `wp-config.php` را پیدا کن و مسیر والد همان فایل را به‌عنوان مسیر دسترسی حساب FTP تنظیم کن. پوشه‌های خدماتی مثل `logs`، `stats`، `public_ftp` و `.htpasswd` با نتیجه `skipped_non_web_directory` در Trace مشخص می‌شوند و برنامه وقتش را صرف جست‌وجوی وردپرس داخل آن‌ها نمی‌کند.

برای یک جمع‌بندی حرفه‌ای از کل جست‌وجو، عبارت `checkpoint=website_discovery_summary` را پیدا کن. `inspected_paths` مسیر حرکت واقعی، `skipped_paths` پوشه‌های کنارگذاشته‌شده همراه دلیل، `denied_paths` مسیرهای بدون مجوز، `empty_paths` پوشه‌های خالی، `conclusion` نتیجه فنی و `recommended_action` اقدام پیشنهادی را یکجا نشان می‌دهد.

اگر `diagnosis_code` برابر `FTP_DOCUMENT_ROOT_EMPTY` بود، این متن را برای پشتیبانی هاست بفرست: «اتصال FTP موفق است، ولی `/public_html` برای کاربر FTP صفر فایل نشان می‌دهد. لطفاً FTP root این کاربر را روی پوشه‌ای بگذارید که فایل `wp-config.php` سایت داخل آن است.» این مشکل با تغییر Remote root داخل برنامه حل نمی‌شود، چون فایل‌ها اصلاً در محدوده قابل‌مشاهده حساب نیستند.

همین لاگ‌های برنامه و `[KEYPOINT]`ها به‌صورت زنده در صفحه **Logs** هم نمایش داده می‌شوند. رکوردهای `APP` از فایل لاگ برنامه و رکوردهای `JOB` از تاریخچه پایدار Job هستند. زمان با میلی‌ثانیه، سطح خطا، نام ماژول و نام Thread نمایش داده می‌شود تا خطاهای Workerها راحت‌تر از رویدادهای رابط کاربری جدا شوند.

## اسکن سایت بعد از اتصال موفق

وقتی در صفحه **Servers** پیام `WordPress discovered` را دیدی، اتصال به‌صورت خودکار به اسکنر فرستاده می‌شود. بعد وارد **Scan > Website** شو و روی **Scan website** کلیک کن. اسکن فقط مشخصات فایل‌های تصویری داخل `wp-content/uploads` را می‌خواند و در این مرحله هیچ فایلی را دانلود، آپلود، حذف یا جایگزین نمی‌کند. بعد از پایان، فهرست تصاویر و تعداد آن‌ها نمایش داده می‌شود. اگر قبل از اتصال روی دکمه بزنی، برنامه به‌جای بی‌واکنش ماندن می‌گوید اول در صفحه Servers اتصال را با **Test & discover** تأیید کن.

## ساخت EXE از روی کد پروژه (برای توسعه‌دهنده‌ها)

اگر به‌جای نسخه آماده، سورس پروژه را دانلود کرده‌ای، برای Build کردن باید **Python 3.11 یا جدیدتر** روی ویندوز نصب باشد. موقع نصب Python حتماً گزینه **Add python.exe to PATH** را فعال کن، بعد پنجره قبلی را ببند و روی فایل `build_windows.bat` دابل‌کلیک کن. اسکریپت هم `py.exe` و هم `python.exe` را شناسایی می‌کند، بررسی‌های لازم برای ساخت را انجام می‌دهد و در پایان فایل زیر ساخته می‌شود:

```text
dist\ImageOptimizer.exe
```

این مرحله برای کاربر عادی لازم نیست؛ دانلود نسخه آماده راحت‌تر است.

اگر خطای `Python 3.11 or newer was not found` دیدی، یعنی Python نصب نیست یا داخل PATH قرار نگرفته. Python را از [وب‌سایت رسمی Python](https://www.python.org/downloads/windows/) نصب کن و دوباره `build_windows.bat` را اجرا کن.

برای ساخت برنامه لازم نیست Git نصب داشته باشی؛ پوشه‌ای که از فایل ZIP گیت‌هاب Extract کرده‌ای هم قابل استفاده است. اگر Git نصب نباشد، پیام `tracked-file audit was skipped` فقط یک اطلاع‌رسانی است و Build ادامه پیدا می‌کند.

تست کامل توسعه‌دهنده‌ها موقع دابل‌کلیک اجرا نمی‌شود، چون بعضی از آن تست‌ها ممکن است روی نسخه‌های مختلف Windows یا Pillow مشکل جداگانه داشته باشند و جلوی ساخته‌شدن EXE را بگیرند. اگر خودت توسعه‌دهنده‌ای و می‌خواهی Build فقط بعد از رد شدن همه تست‌ها انجام شود، PowerShell را باز کن و این دستور را بزن:

```powershell
.\scripts\build_windows.ps1 -RunTests
```

ساخت اول به خاطر دانلود کتابخانه‌ها زمان بیشتری می‌برد. از دفعه دوم، با اجرای
`build_windows.bat` محیط نصب‌شده و کش سالم PyInstaller دوباره استفاده می‌شود؛
پس دیگر `pip` بی‌دلیل آپدیت نمی‌شود و ساخت خیلی سریع‌تر تمام می‌شود.

فقط اگر عمداً ساخت کاملاً تازه یا نصب دوباره کتابخانه‌ها را می‌خواهی، این دستور
را در PowerShell اجرا کن:

```powershell
.\scripts\build_windows.ps1 -Clean -RefreshDependencies
```

</div>
